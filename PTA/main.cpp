#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include"PAT (Basic Level) Practice.h"
#include"datastruct.h"
int main(int argc, char const *argv[])
{
	//PAT1002(argc, argv);
	List_ppt(argc, argv);
	//char *a = (char*)"hll";
	//printf("%s", a);
	return 0;
}